帮我用python写一段图片像素颜色相似度比较的代码，


一、自定义基本单元：
我在代码中可以自定义枚举来决定比对大小的基本单元，如：
枚举值=P，表示点为基本单元
枚举值=X，表示行为基本单元
枚举值=Y，表示列为基本单元

结合变量n&m，n&m_枚举值：决定一个基本单元，
举例如下：
10_X：表示10行，10为n的值
10&10_P：10*10矩阵
10_Y：表示10列，10为n的值
10&5_P：表示10*5的矩阵
8&9_P：表示8*9的矩阵


像素基本单元比较顺序：
枚举值=P时，以左上角为中心店，由左向右，换行，继续由左向右直至最后
枚举值=X时，由左向右直至最后
枚举值=Y时，由上向下直至最后
标识ID为为循环游标

二、像素颜色相似度定义：
1.像素差值决定因素：pixel1
pixel1 = image1.load()[x, y]，按pixel1的三个色差来

2.相似度：差值在基本单元里的平均值

三、输出信息：
1.输出每个基本单元的信息以及相似度，格式如下：
单元信息：10*10的矩阵
ID:(游标)1：相似度100%
ID:(游标)2：相似度86%


四：其他变量：
两张图片的位置：
image1 = Image.open("../down/img/001.png")
image2 = Image.open("../down/img/001_yy.png")


针对以上信息，你还有什么不清楚的么，可以提出来，如果清楚可以直接输出代码